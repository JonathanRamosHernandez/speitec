import 'package:flutter/material.dart';
class powerpage extends StatelessWidget{
  @override
Widget build (BuildContext context){
    return Scaffold(
      appBar: new AppBar(title: Text('powerpage'),),
    body: new Column(
      children: <Widget>[
        new Text('estamos en powerpage')
      ],
    ),

    );
  }
}